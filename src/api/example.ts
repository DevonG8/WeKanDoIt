// export async function GET/POST/PUT/DELETE(req: Request) {
//   try {
//     const user = await getUser(req)   // 1. Verify auth
//     // 2. Validate input
//     // 3. Query Supabase
//     // 4. Return response
//   } catch (error) {
//     return handleError(error)          // Always catch errors
//   }
// }
